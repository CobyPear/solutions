const duplicateEncode = (word) => {
    // ...
  // set word to all lowercase
  // read through the word
  // remove char at 0
  // if character at 0 !== any other chars in word, push '(' to result
  // else, push ')' to result
  

}

console.log(duplicateEncode("din"))
console.log(duplicateEncode("Success"))